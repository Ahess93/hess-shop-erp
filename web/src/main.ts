// Hess Solutions Shop ERP — web entry point
// This file will be replaced with a React root in Phase 2.
// For now it renders a placeholder to confirm the dev server is running.

const app = document.getElementById('app');
if (app) {
  app.innerHTML = `
    <div style="font-family:sans-serif;text-align:center;padding:4rem;">
      <h1>Hess Solutions — Shop ERP</h1>
      <p>Phase 0 scaffold is running. UI will be built in Phase 2.</p>
    </div>
  `;
}
